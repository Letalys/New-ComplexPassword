# New-ComplexPassword
A customizable PowerShell module that generates secure, complex passwords based on user‑defined parameters. It supports fine‑grained control over character sets, minimum requirements, repetition rules, and provides a strength score for each generated password.
